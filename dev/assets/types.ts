export type CardStyle = {
    top: number,
    left: number,
    width: string,
    height: string,
    backgroundColor: string
}

export type CardAnimation = {
    transition?: string,
    transform?: string,
    boxShadow?: string
}